#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - Main Entry
 * Description: computes the absolute value of an integer.
 * @r: input
 * Return: Always (0) in success
 */

int _abs(int r)
{
	return (abs(r));
}
