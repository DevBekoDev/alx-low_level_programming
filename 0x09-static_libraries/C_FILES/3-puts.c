#include "main.h"
#include <stdio.h>

/**
 * _puts - Entry
 * Description:  a function that prints a string,
 * followed by a new line, to stdout.
 * @str: pointer to a string
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
